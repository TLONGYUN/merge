package com.tlong.merge.repository.primary;

import com.tlong.merge.domain.primary.WebSlideshow;
import com.tlong.merge.repository.BaseRepository;

public interface WebSlideshowRepository extends BaseRepository<WebSlideshow,Long> {
}
